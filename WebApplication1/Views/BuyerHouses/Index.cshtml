@model IEnumerable<WebApplication1.Models.BuyerHouse>

@{
    ViewData["Title"] = "Index";
}
<br /><br /><br /><br /><br />
<h4>Index</h4>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.House)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.BuyerID)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.House.BuyPrice)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.BuyerID)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.BuyerHouseID">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.BuyerHouseID">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.BuyerHouseID">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
